{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "bun install && bunx turbo run build --filter=@eiab/demo"
  },
  "deploy": {
    "startCommand": "bun run --filter=@eiab/demo start"
  }
}
